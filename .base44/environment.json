{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret key for signing JWT authentication tokens",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    },
    {
      "name": "GOOGLE_CLIENT_ID",
      "description": "Google OAuth 2.0 Client ID from Google Cloud Console > Credentials. Add the preview origin as an Authorized JavaScript origin."
    }
  ]
}
